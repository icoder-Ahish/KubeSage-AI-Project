apiVersion: v1
kind: Service
metadata:
  name: k8sgpt-service
  labels:
    app: k8sgpt-service
spec:
  selector:
    app: k8sgpt-service
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
  type: ClusterIP
